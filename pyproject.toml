[project]
name = "agent-ia"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-adk[extensions]>=2.8.0",
    "ollama>=0.6.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
